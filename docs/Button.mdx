---
status: ready
menu: Components
---

import { Button, ButtonGroups } from '../src/components/Button';
import { IconAirplane } from '../src/icons';

# Button

```js
import { Button } from '@peru-com/ui';
```
## Basic usage

<Playground>
  <Button>Default</Button>
</Playground>

## Variants

Set variants using ***variant*** prop.

<Playground>
  <ButtonGroups>
    <Button>Default</Button>
    <Button variant="primary">Primary</Button>
    <Button variant="secondary">Secondary</Button>
    <Button variant="info">Info</Button>
    <Button variant="success">Success</Button>
    <Button variant="danger">Danger</Button>
    <Button variant="warning">Warning</Button>
  </ButtonGroups>
</Playground>

## Loading

To indicate a button is loading, use the ***isLoading*** prop.

<Playground>
  <ButtonGroups>
    <Button isLoading>Default</Button>
    <Button isLoading variant="primary">Primary</Button>
    <Button isLoading variant="secondary">Secondary</Button>
    <Button isLoading variant="info">Info</Button>
    <Button isLoading variant="success">Success</Button>
    <Button isLoading variant="danger">Danger</Button>
    <Button isLoading variant="warning">Warning</Button>
  </ButtonGroups>
</Playground>

## Disabled

To indicate a button is disabled, use the ***isDisabled*** prop.

<Playground>
  <ButtonGroups>
    <Button isDisabled>Default</Button>
    <Button isDisabled variant="primary">Primary</Button>
    <Button isDisabled variant="secondary">Secondary</Button>
    <Button isDisabled variant="info">Info</Button>
    <Button isDisabled variant="success">Success</Button>
    <Button isDisabled variant="danger">Danger</Button>
    <Button isDisabled variant="warning">Warning</Button>
  </ButtonGroups>
</Playground>

## Outlined

To indicate a button is disabled, use the ***isOutlined*** prop.

<Playground>
  <ButtonGroups>
    <Button isOutlined>Default</Button>
    <Button isOutlined variant="primary">Primary</Button>
    <Button isOutlined variant="secondary">Secondary</Button>
    <Button isOutlined variant="info">Info</Button>
    <Button isOutlined variant="success">Success</Button>
    <Button isOutlined variant="danger">Danger</Button>
    <Button isOutlined variant="warning">Warning</Button>
  </ButtonGroups>
</Playground>

## Size

Set sizes using ***size*** prop.

<Playground>
  <ButtonGroups>
    <Button size="small">Small</Button>
    <Button size="normal">Normal</Button>
    <Button size="medium">Medium</Button>
    <Button size="large">Large</Button>
  </ButtonGroups>
</Playground>

## Display

To take full width of the parent element, use ***fullWidth*** prop.

<Playground>
  <ButtonGroups>
    <Button fullWidth size="small">Small</Button>
    <Button fullWidth size="normal">Normal</Button>
    <Button fullWidth size="medium">Medium</Button>
    <Button fullWidth size="large">Large</Button>
  </ButtonGroups>
</Playground>

## Custom width

Custom button width, use ***width*** prop.

<Playground>
  <ButtonGroups>
    <Button width={400}>Normal 400</Button>
    <Button width={600}>Normal 600</Button>
    <Button width={800}>Normal 800</Button>
  </ButtonGroups>
</Playground>

## Events onClick

Do action on event, use ***onClick*** prop.

<Playground>
  <Button onClick={() => console.log('Click')}>Default</Button>
</Playground>

## Icons

```js
import { IconAirplane } from '@peru-com/ui';
```

<Playground>
  <ButtonGroups>
    <Button 
      variant="primary"
      icon={<IconAirplane />}
      isOutlined
    >
      PRIMARY
    </Button>
    <Button 
      variant="primary"
      icon={<IconAirplane />}
    >
      PRIMARY OUTLINED
    </Button>
  </ButtonGroups>
</Playground>

## Icons position

Set your icon position, use ***iconPos*** prop.

```js
import { IconAirplane } from '@peru-com/ui';
```

<Playground>
  <ButtonGroups>
    <Button 
      variant="primary"
      icon={<IconAirplane />}
      iconPos="left"
    >
      PRIMARY LEFT
    </Button>
    <Button 
      variant="primary"
      icon={<IconAirplane />}
      iconPos="right"
    >
      PRIMARY RIGHT
    </Button>
  </ButtonGroups>
</Playground>

## Props

<Props of={Button} />